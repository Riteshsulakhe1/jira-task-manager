import React from "react";
import { Grid } from "@mui/material";

const Filters = () => {

    return (
        <Grid item={true} sx={{ height: '3rem', marginBottom: '0.25rem' }} xs={12}>Filters..</Grid>
    )
};
export default Filters;